import 'package:flutter/material.dart';
class AnimatedMarkerMap extends StatelessWidget {
  const AnimatedMarkerMap({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}